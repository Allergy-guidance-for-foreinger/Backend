services:
  redis:
    image: redis:7-alpine
    container_name: mealguide-local-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-local-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  redis-local-data:
